@model IEnumerable<EDnevnik.Models.Obavijest>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nastavnik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nastavnik.ImePrezime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ObavijestId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ObavijestId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ObavijestId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
